import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // --- COLORES COMPARTIDOS / MARCA ---
  static const Color primaryRed = Color(0xFFDC2E2E);
  static const Color accentBlue = Color(0xFF008BFF);

  // --- MODO CLARO (LIGHT MODE) ---
  static const Color lightBackground = Color(0xFFF0F3F7);
  static const Color lightCard = Color(0xFFFFFFFF);
  static const Color lightHeader = Color(0xFF1C2431);
  static const Color lightInput = Color(0xFFE1E5EA);

  // Textos Light
  static const Color lightTextPrimary = Color(0xFF1C2431);
  static const Color lightTextSecondary = Color(0xFF6B7280);
  static const Color lightTextOnWhite = Color(0xFF111827);

  // --- MODO OSCURO (DARK MODE) ---
  static const Color darkBackground = Color(0xFF121824);
  static const Color darkCard = Color(0xFF1C2431);
  static const Color darkHeader = Color(0xFF0B0E14);
  static const Color darkInput = Color(0xFF2C3749);

  // Textos Dark
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFA1A1AA);
  static const Color darkTextOnDark = Color(0xFFF3F4F6);

  // --- POKÉMON TYPES (TAGS) ---
  static const Color typeGrass = Color(0xFF5EBD67);
  static const Color typePoison = Color(0xFFA754A8);
  static const Color typeFire = Color(0xFFF38334);
  static const Color typeWaterFlying = Color(0xFF88A1E7);
}
